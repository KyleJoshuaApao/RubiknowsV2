[
  "## Workaround confirmed \u2705\n\nWe found a temporary workaround that confirms the root cause analysis.\n\n### Fix\n\nChanging `handler` from `'launcher.launcher'` to `'launcher.js'` in `src/index.ts` resolves the issue. No changes to the launcher's export format (`exports.launcher = launcher`) were needed \u2014 the Vercel Rust bootstrap correctly resolves the named `launcher` export from `launcher.js` as a CommonJS module.\n\n### Verification\n\n1. Built a minimal PHP test project with `vercel-php@0.9.0` (unmodified from npm)\n2. Added a `buildCommand` script that patches `handler: 'launcher.launcher'` \u2192 `handler: 'launcher.js'` in the installed runtime's `dist/index.js` at build time\n3. Deployed to an isolated Vercel test project\n4. **Result:** HTTP 200 with correct PHP response \u2014 no more `ERR_MODULE_NOT_FOUND`\n\nThe patch script finds the runtime at:\n```\n/vercel/path0/.vercel/builders/node_modules/vercel-php/dist/index.js\n```\n\n### Suggested fix for vercel-php\n\nIn [`src/index.ts`](https://github.com/vercel-community/php/blob/master/src/index.ts) line 127:\n\n```diff\n- handler: 'launcher.launcher',\n+ handler: 'launcher.js',\n```\n\nThis changes the handler from the AWS Lambda `<module>.<export>` convention to the file-path format that the current Vercel Node bootstrap expects. The launcher's existing `exports.launcher = launcher` (CommonJS named export) is compatible \u2014 no export format change required.\n\n### Temporary workaround for affected projects\n\nAdd a `build-patch.sh` to your project:\n\n```bash\n#!/bin/bash\nset -e\nPHP_FILE=$(find /vercel -name \"index.js\" -path \"*vercel-php*\" 2>/dev/null | head -1)\nif [ -n \"$PHP_FILE\" ]; then\n  sed -i \"s/handler: 'launcher.launcher'/handler: 'launcher.js'/g\" \"$PHP_FILE\"\n  echo \"Patched vercel-php handler\"\nfi\n```\n\nAnd in `vercel.json`:\n```json\n{\n  \"buildCommand\": \"bash build-patch.sh\",\n  \"functions\": {\n    \"api/index.php\": {\n      \"runtime\": \"vercel-php@0.9.0\"\n    }\n  }\n}\n```\n\nHope this helps others hitting the same issue. Happy to open a PR if the maintainers confirm the fix direction."
]